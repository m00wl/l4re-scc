local L4     = require("L4");
local loader = L4.default_loader;

-- l4re-base:
loader:startv({}, "rom/pointer-chasing");
loader:startv({}, "rom/video-pipeline", "rom/video.yuv");

-- -- l4re-sc:
-- local mbw_t = L4.SC_types.Mbw_sc;
-- local sc    = L4.Env.factory:create(L4.Proto.Sched_constraint, mbw_t, 50, 50);
-- local sched = L4.Env.user_factory:create(L4.Proto.Scheduler, 0xFF, 0x0);
-- sched:set_global_sc(sc);
-- loader:startv({scheduler = sched}, "rom/pointer-chasing");
-- loader:startv({}, "rom/video-pipeline", "rom/video.yuv");
